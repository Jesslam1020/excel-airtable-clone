{
  "name": "excel-airtable-clone",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "deploy:vercel": "vercel --prod",
    "deploy:build": "npm run build && npm run start",
    "deploy:preview": "vercel",
    "deploy:auto": "git add . && git commit -m \"🚀 Auto-deploy\" && git push origin main",
    "deploy:manual:windows": "scripts\\manual-deploy.bat",
    "deploy:manual:unix": "chmod +x scripts/manual-deploy.sh && scripts/manual-deploy.sh",
    "setup:windows": "scripts\\setup.bat",
    "setup:unix": "chmod +x scripts/setup.sh && scripts/setup.sh",
    "quick-deploy:windows": "scripts\\quick-deploy.bat",
    "quick-deploy:unix": "chmod +x scripts/quick-deploy.sh && scripts/quick-deploy.sh",
    "firebase:deploy": "firebase deploy",
    "firebase:rules": "firebase deploy --only firestore:rules",
    "export": "next build && next export"
  },
  "dependencies": {
    "firebase": "^12.2.0",
    "html2canvas": "^1.4.1",
    "jspdf": "^2.5.2",
    "jspdf-autotable": "^3.8.4",
    "next": "14.2.3",
    "react": "^18",
    "react-dom": "^18",
    "react-dropzone": "^14.3.8",
    "recharts": "^2.12.7",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "14.2.3",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
